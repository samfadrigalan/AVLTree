/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
package avltreeapp;
import java.util.Queue;
/**
 * @file AVLTree.java
 * @author Samantha Fadrigalan
 * Description: Generic AVL Implementation 
 * Course: CSC 3102
 */
public class AVLTree<T extends Comparable<T>> {
    /**
     * Inserts node z to a BST
     * @param x
     * @param z 
     */
    /*public void insert(Node<T> x, Node<T> z){
        if(x.left.key > z.key){
            if(x.hasLeft())
                insert(x.left, z);
            else
                x.left = z;
        }
        else if(x.key < z.key){
            if(x.hasRight())
                insert(x.right, z);
            else
                x.right = z;
        }
    }*/
}
